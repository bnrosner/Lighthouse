# Create the classes
School:
  name: string
  location: string

Course:
  name: string
  course_number: string
  school_id: integer

# Create the tutors and students
User:
  name: string
  graduation_year: string
  school_id: integer
  email: string
  password_digest: string

Tutor:
  user_id: integer
  course_id: integer

Student:
  user_id: integer
  course_id: integer
